# QA-Abgleich F-08 · Kartengerüst: Skalen, Raster, Reviere

Geprüft auf `feature/F-08-kartengeruest`, Stand nach `2412c0a` (Tests rot) und `f5e4903`
(Umsetzung).

## Geprüfte Punkte

- **Umfang laut `features/F-08-kartengeruest.md`.** `src/lib/layout/scales.ts` mit `VIEWBOX`,
  `PLOT`, `domainMaxOf`, `xOf`, `yOf`, `ticksOf`, `regionRects` sowie
  `src/lib/components/Map/{MapCanvas,Regions,Grid,Axes}.svelte` sind vorhanden und wortgleich
  zu den vorgegebenen Signaturen. `git diff 2412c0a..HEAD --stat` betrifft ausschließlich
  diese fünf Dateien plus `src/routes/+page.svelte` (Einbindung von `MapCanvas` ins
  Grundgerüst aus F-01) — nichts aus „Nicht Teil dieses Features" (Feature-Kreise/Kanten
  F-09/F-10, Zoom/Pan F-12, Zeichenerklärung) wurde umgesetzt.
- **`VIEWBOX`/`PLOT`.** Wortgleich `{width:1000, height:700}` und
  `{left:80, right:960, top:40, bottom:620}` wie in der Featurebeschreibung vorgegeben.
- **`domainMaxOf` (FR-26).** Liefert 22 für die leere Karte sowie für Karten ohne Wert über 21;
  wächst korrekt auf `größterWert + 1`, auch wenn nur `impact` oder nur `effort` den Bereich
  sprengt. Live geprüft: Feature mit `effort=34` verschiebt die Achsen sichtbar auf
  Höchstwert 34, zusätzlicher Teilstrich 34 erscheint auf beiden Achsen, alle Positionen
  skalieren mit (Screenshot im Nachttafel-Zustand bestätigt).
- **`xOf`/`yOf` (FR-20, FR-21).** `xOf(0,22)=80`, `xOf(22,22)=960`, `yOf(0,22)=620`,
  `yOf(22,22)=40` exakt wie im Akzeptanzkriterium gefordert. Y sinkt mit steigendem `impact`
  (FR-21, hoher Nutzen liegt oben) — X-Achse ist Effort, Y-Achse ist Impact (FR-20).
- **`ticksOf` (FR-26).** Schätzreihe 1,2,3,5,8,13,21 für die leere Karte; ergänzt `domainMax-1`
  korrekt sowohl bei „glatten" (35 → zusätzlich 34) als auch bei knapp über 21 liegenden
  Obergrenzen (23 → zusätzlich 22); lässt Werte der Reihe oberhalb von `domainMax` weg.
- **Revier-Grenze (features/README.md, „Zwei Präzisierungen…").** `regionRects` teilt exakt
  bei `domainMax / 2` über `xOf`/`yOf`, nicht bei einer festen Pixelgrenze — bestätigt durch
  den Test „verschiebt die Reviergrenze mit, wenn domainMax wächst" und live durch das
  effort=34-Beispiel, bei dem die Reviergrenze sichtbar mitwandert statt bei x=520/y=330 zu
  verharren. Bei `domainMax=22` liegen die Grenzen exakt bei x=520, y=330 wie in
  `design/03-seekarte.html` gezeichnet.
- **`regionRects` deckt die Plotfläche lückenlos und überlappungsfrei ab** (Unit- und
  E2E-Test, zusätzlich live an den Bounding Boxes der vier `data-testid`-Rechtecke geprüft).
- **Leitplanke 1 (`src/lib/layout/` frameworkfrei).** `architecture.test.ts` prüft
  `scales.ts` und die vier Komponenten auf Importe aus `svelte`/`$app`, auf `window`/
  `document` sowie auf Hexfarben — 0 Treffer, Suite grün. Manuell nachgesehen: `scales.ts`
  enthält ausschließlich Zahlen- und `FeatureMap`-Typbezüge, keine Farb- oder DOM-Referenz.
- **Live-Abgleich mit `design/03-seekarte.html` (Tag- und Nachttafel).** App über
  `npm run build` + `npm run preview` gestartet, im Browser geöffnet (LocalStorage vor jedem
  Lauf geleert, um Alttafel-Reste aus vorherigen Sitzungen auszuschließen — Standard ist
  korrekt „Tag", unabhängig von einer evtl. dunklen Systemeinstellung, design/README.md).
  Verglichen: Reviertönung (`--shallow/--deep/--flat/--hazard`), Schraffur auf „Vermeiden"
  (`#hatch`, `--magenta`, `--hatch-opacity`), Rastergitter nur auf Teilstrichwerten
  (`--grid`), Rahmen (`--rule`), Teilstrichbeschriftung (Azeret Mono 10,5 px, x-Zeile
  y=638 mittig, y-Spalte x=68 rechtsbündig), Achsentitel (Fraunces kursiv gesperrt, EFFORT
  mittig (520,664), IMPACT gedreht mittig (32,330)), Reviernamen (Fraunces kursiv,
  `letter-spacing:.38em`, `--region`, oben 34 px unter der Rahmenkante, unten 20 px darüber).
  Alles deckungsgleich mit dem Entwurf. Tafelwechsel per Klick auf „Nacht" geprüft: Rahmen,
  Raster und alle vier Reviere wechseln sichtbar auf die Nachttafel-Töne, kein Element bleibt
  hell (E2E-Test vergleicht zusätzlich die berechneten CSS-Werte gegen die Token aus
  `src/app.css` und ist grün). Über `document.fonts` bestätigt: Fraunces italic, Azeret Mono
  und Karla sind geladen (`status: loaded`).
- **Breakpoints 375/834/1440 px.** Die Fensterbreite ließ sich in der interaktiven
  Browsersitzung dieser Umgebung nicht auf diese Werte verkleinern (`resize_window` meldete
  Erfolg, `window.innerWidth` blieb bei der physischen Bildschirmbreite 2560 px — technische
  Einschränkung der QA-Umgebung, keine Eigenschaft der Anwendung). Ersatzweise wurden die drei
  Breakpoints über die bereits vorgelegten E2E-Tests geprüft, die in echten Chromium-Instanzen
  mit `page.setViewportSize` exakt auf 375/834/1440 px laufen und Rahmen, Reviernamen und das
  Ausbleiben von horizontalem Überlauf prüfen (`e2e/F-08-kartengeruest.spec.ts:187-203`) —
  alle drei grün. Die Karte selbst nutzt `preserveAspectRatio="xMidYMid meet"` in einem
  Container mit `width:100%; height:100%` (MapCanvas.svelte), skaliert also unabhängig von der
  Containerbreite proportional; interne Positionen/Abstände ändern sich zwischen den
  Breakpoints nicht.
- **Test-Diff gegen `2412c0a` (`F-08 · Tests (rot)`).** `git diff 2412c0a..HEAD -- '*.test.ts'
  e2e/` liefert keine Ausgabe — keine einzige Testdatei wurde verändert.
- **Akzeptanzkriterien.** Alle sechs AK aus F-08.md sind einem Test zugeordnet: Teilstriche/
  Reviergrenzen mittig (Unit „Randwerte"/„Reviergrenze", E2E „sieben Teilstriche"), effort=34
  verschiebt domainMax (Unit + E2E), `xOf`/`yOf`-Randwerte (Unit, wortgleich zitiert), Reviere
  lückenlos/überlappungsfrei (Unit + E2E), Nachttafelwechsel (E2E), kein Hexwert unter `Map/`
  (Architekturtest). Kein AK ohne Test gefunden.
- **Festlegung des Orchestrators zur Reviername-Position.** `Regions.svelte` zentriert
  `region-lbl` über `rect.x + rect.width/2` (bei `domainMax=22`: x=300 für die linke Spalte),
  nicht auf die feste Entwurfskoordinate `x="330"`. Das entspricht der in
  `features/STATUS.md`, Abschnitt „Entscheidungen des Orchestrators", dokumentierten
  Festlegung und ist laut Auftrag kein Befund.

## Befund

Keiner. Die Umsetzung entsprach bei Prüfungsbeginn bereits allen drei Quellen; es waren keine
Korrekturen am Branch nötig.

## Quellenwidersprüche

Keine, die nicht bereits vom Orchestrator entschieden wären (Reviername-Position, siehe oben).

## Suitenlauf (vollständig, vor Abschluss)

- `npm run test`: 301/301 grün (16 Testdateien).
- `npm run test:e2e`: 18/18 grün (Chromium).
- `npm run check`: 0 Fehler, 0 Warnungen (347 Dateien).
- `npm run build`: erfolgreich.
- `npm run coverage`: Gesamt 99,08 % Stmts/98,37 % Branch/100 % Funcs; `dsl/` 100 % über alle
  Metriken, `graph/` 100 %/95,23 %/100 %/100 % — beide deutlich über der Vorgabe aus NFR-41
  (≥ 90 %). `src/lib/layout/` ist reine Rechenlogik ohne NFR-41-Pflicht, aber ebenfalls voll
  von `scales.test.ts` abgedeckt (18 Tests, alle Randfälle aus dem Feature).

## Ergebnis

Keine Abweichungen.
