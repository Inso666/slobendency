# QA · F-16 Kontextmenü und Verbindungsvorgang

Geprüft auf `feature/F-16-verbindungsvorgang` (Tests (rot) `992513e`, Umsetzung `423126c`
„F-16 · Kontextmenü und Verbindungsvorgang"), gegen `features/F-16-verbindungsvorgang.md`,
`PRD.md` und `design/03-seekarte.html`/`design/README.md`, in dieser Reihenfolge (CLAUDE.md,
Abschnitt „QA-Abgleich"). Zusätzlich berücksichtigt: `features/STATUS.md`, Abschnitt
„Entscheidungen des Orchestrators", die beiden F-16 betreffenden Einträge „Zyklus-Warnung
AK-08 gehört zu F-23, nicht zu F-16 (06.09.)" und „Fehlender Hover vor Labelprüfung in F-16
(07.09.)".

## Vorab: aufgetragene Testkorrektur

`e2e/F-16-verbindungsvorgang.spec.ts`, Test „übernimmt eine optionale Beschriftung in die neue
Beziehung", prüfte `edge-label-a-c-relates` unmittelbar nach dem Anlegen auf Sichtbarkeit und
Text — ohne vorherigen Hover oder Selektion. FR-45 legt aber fest, dass Kantenlabels nur bei
Selektion oder Hover eingeblendet werden (bereits durch F-10/F-11 umgesetzt und dort selbst
getestet: `e2e/F-10-kanten.spec.ts` Zeile 218 `toBeHidden()` im Grundzustand,
`e2e/F-11-selektion.spec.ts` Zeile 289 `toBeVisible()` erst nach Selektion, Zeile 260
`.hover({ force: true })` als etabliertes Hover-Muster). Wie vom Orchestrator festgelegt: vor
der Sichtbarkeitsprüfung einen `hover({ force: true })` auf die neue Kante (`edge-a-c-relates`)
ergänzt, analog zum Muster aus F-10/F-11. Prüfabsicht (Beschriftung wird unverändert übernommen)
und Erwartungswert unverändert — reine Ergänzung eines fehlenden Zwischenschritts, keine neue
Prüfabsicht.

`git diff 992513e HEAD -- e2e/F-16-verbindungsvorgang.spec.ts` zeigt nach der Korrektur exakt
diese eine Ergänzung (Hover-Aufruf plus Begründungskommentar) und sonst keine Abweichung vom
Stand des Tests-(rot)-Commits.

## 1. features/F-16-verbindungsvorgang.md

Alle Akzeptanzkriterien geprüft, mit Ausnahme des laut Orchestrator-Entscheidung vom 06.09.
F-23 vorbehaltenen Warnungsanteils von AK-08:

- Rechtsklick öffnet das Kontextmenü an der Zeigerposition, Browsermenü unterdrückt (geprüft
  am laufenden Programm und per E2E-Test) — erfüllt.
- Startpunkt nach „Als Start verwenden" dauerhaft markiert (gestrichelte Kontur `--magenta`,
  `connect-start-<id>`), bleibt über eine unbeteiligte Aktion (Öffnen/Schließen eines weiteren
  Menüs) hinweg bestehen — erfüllt.
- „Als Ziel verwenden" auf dem Startfeature deaktiviert (INT-03) — erfüllt.
- Vorhandenes Tripel wird mit „Beziehung existiert bereits" abgelehnt, Dialog bleibt offen,
  gelingt danach im selben Dialog mit anderer Art — erfüllt.
- A → B → A lässt sich anlegen (INT-05, F-16-Anteil: Anlegen, keine Warnung) — erfüllt; die
  sichtbare Warnung selbst ist laut Orchestrator-Entscheidung F-23 vorbehalten und hier weder
  gebaut noch getestet (siehe Abschnitt 2 unten, INT-05).
- ESC bricht zuerst den Vorgang ab (Markierung/Hinweisband verschwinden, Selektion bzw.
  Detail-Kartusche bleibt), erst ein zweites ESC hebt die Selektion auf — erfüllt (Unit- und
  E2E-Test).
- Long-Press (~500 ms) öffnet dasselbe Menü, eine kurze Berührung (120 ms) öffnet keins —
  erfüllt.
- Neue Kante erscheint sofort ohne Neuladen, an allen drei Breakpoints 375/834/1440 px —
  erfüllt.

Genannte Dateien und Signaturen vorhanden: `src/lib/components/ContextMenu/ContextMenu.svelte`,
`src/lib/components/RelationDialog/RelationDialog.svelte`, Erweiterung der Kartendarstellung
(`FeatureNodes.svelte`, Startpunktmarkierung) — alle drei vorhanden und mit Inhalt gefüllt.
Abbruchwege aus dem Abschnitt „Abbruch" (ESC, Klick auf freie Fläche, Knopf „Abbrechen" im
Hinweisband, Knopf „Abbrechen" im Dialog) sind alle vier vorhanden und einzeln getestet.

„Nicht Teil dieses Features": Bearbeiten/Löschen bestehender Beziehungen (F-17) — nicht
gebaut, das Kontextmenü bietet nur „Bearbeiten"/„Löschen" für das **Feature** selbst (bereits
aus F-13/F-05-FR-05 bestehende Kanäle, keine Beziehungspflege). Anzeige der Zyklus-Warnung
(F-23) — nicht gebaut; `RelationDialog.svelte` ruft nach erfolgreichem `createRelation()` nur
`onCreated()` auf, ohne Warnhinweis, Fußleiste oder Kantenfärbung zu berühren. Kein Vorgriff.

## 2. PRD.md

- **FR-10 bis FR-15**: wörtlich erfüllt — Kontextmenü auf der Map (Rechtsklick/Long-Press),
  Einträge „Als Start verwenden"/„Als Ziel verwenden", Dialog bei gesetztem Start und Ziel,
  jederzeit abbrechbar (ESC/Klick auf freie Fläche/Abbrechen-Knopf), dauerhafte
  Startmarkierung, Beziehungen ebenso über die Feature-Liste anlegbar (F-14 setzt den Start,
  F-16 führt bis zur angelegten Beziehung fort — eigener E2E-Test).
- **INT-03/INT-04**: `from ≠ to` und Tripel-Eindeutigkeit werden im Aggregat geprüft
  (`src/lib/model/validation.ts`), `RelationDialog.svelte` zeigt die Meldung nur an, prüft
  nichts selbst nach — keine doppelte Fachregel (Leitplanke 3).
- **INT-05**: PRD-Wortlaut „Beziehung wird **angelegt**, aber als Warnung markiert" — der
  F-16-Anteil („angelegt") ist erfüllt und getestet; der Warnungsanteil ist laut
  Orchestrator-Entscheidung vom 06.09. F-23 vorbehalten und dort zu prüfen. Kein Widerspruch,
  siehe Entscheidung.
- **FR-45**: „Kantenlabels werden nur bei Selektion oder Hover eingeblendet" — durch die
  aufgetragene Testkorrektur jetzt auch im F-16-Test selbst beachtet; am laufenden Programm
  bestätigt (Beschriftung der neuen Kante erscheint erst nach Hover, vorher verborgen wie in
  F-10/F-11).
- **UI-12**: Long-Press löst das Kontextmenü aus — erfüllt, per Timer (500 ms) in
  `FeatureNodes.svelte`, mit Abbruch bei `touchend`/`touchmove`/`touchcancel` vor Ablauf.
- **UI-16**: Menüeinträge/Trefferflächen ≥ 44 × 44 px — erfüllt (`min-height: 44px`,
  `width: 100%`, E2E-Test misst jeden Menüeintrag).

## 3. design/03-seekarte.html und design/README.md

`design/README.md` benennt Kontextmenü und Dialog ausdrücklich als „noch zu entwerfen", mit
den bereits feststehenden Bausteinen Rahmen, Kartusche, Panel als Vorgabe. Geprüft am
laufenden Programm (`npm run build && npm run preview`, Playwright-gesteuerte Screenshots) bei
375/834/1440 px sowie Tag- und Nachttafel:

- Beide neuen Oberflächen (`ContextMenu.svelte`, `RelationDialog.svelte`) verwenden die
  bestehende `.cartouche`-Klasse aus `src/app.css` (Doppelrand-Optik, identisch zu
  Detail-Kartusche und Zeichenerklärung) statt eines eigenen Rahmens.
- Ausschließlich Farbtoken aus `app.css` (`--paper`, `--ink`, `--ink-soft`, `--hair`,
  `--hover`, `--magenta`, `--sea`) — keine Literalfarbe gefunden (`grep` über beide
  Komponenten).
- Die drei Knöpfe für die Beziehungsart im `RelationDialog` übernehmen Linienart, Pfeilspitze/
  Endmarke und Farbe unverändert aus `Legend.svelte` (F-10) — dieselbe Signatur, keine zweite
  danebengestellt.
- Startmarkierung: gestrichelte Kontur in `--magenta`, wie im Feature-Dokument gefordert, in
  beiden Tafeln sichtbar (Token wechselt korrekt zwischen Tag- und Nachtwert, geprüft per
  E2E-Test und Screenshot).
- Hinweisband „Ziel wählen — ESC bricht ab" als schlanke Kartusche oben mittig über der
  Kartenfläche, mit denselben Token wie jede andere Kartusche.
- Tag- und Nachttafel funktionieren für beide neuen Komponenten (Screenshots bei allen drei
  Breakpoints in beiden Tafeln geprüft: Kontextmenü, Startmarkierung, Dialog).
- Bei 375 px wechselt der Dialog auf Vollbild (`100vw`/`100vh`, `inset: 0`), das Kontextmenü
  bleibt an den Bildschirmrand geklemmt (`MARGIN`-Berechnung in `ContextMenu.svelte`) — an
  keinem der drei Breakpoints ragt eines der beiden Elemente über den sichtbaren Bereich
  hinaus.

Keine Abweichung gefunden. (Beobachtung ohne Befundcharakter: `npm run check` meldet für
`ContextMenu.svelte`/`RelationDialog.svelte` einige Svelte-A11y-Hinweise, z. B. „Redundant role
'dialog'" und „Elements with the 'menu' interactive role must have a tabindex value" — 0 Fehler,
nur Warnungen, und dasselbe Muster (natives `<dialog>` mit explizitem `role`) findet sich bereits
unverändert in `FeatureModal.svelte` aus F-13. Keine der drei Quellen verlangt eine bestimmte
ARIA-Struktur über die geprüften Rollen/Namen hinaus; kein Verstoß gegen FR-, INT-, DSL-, NFR-
oder AK-Wortlaut.)

## 4. Tests selbst

`git diff 992513e HEAD -- e2e/F-16-verbindungsvorgang.spec.ts src/lib/store/selection.test.ts`
zeigt außer der oben beschriebenen, vom Orchestrator freigegebenen Hover-Ergänzung keine
weitere Abweichung vom Stand aus `F-16 · Tests (rot)`. Kein Test wurde abgeschwächt,
übersprungen oder gelöscht. Jedes Akzeptanzkriterium aus `features/F-16-verbindungsvorgang.md`
hat mindestens einen zugeordneten Test (siehe Abschnitt 1). Die 13 Unit-Tests der
Zustandsmaschine in `selection.test.ts` sind unverändert und laufen grün.

## Quellenwidersprüche

Keine offenen. Die beiden in `features/STATUS.md` bereits entschiedenen Punkte (Zyklus-Warnung
gehört zu F-23; fehlender Hover im F-16-Test) sind Klarstellungen des Orchestrators, keine
neuen Widersprüche — beide wie vorgegeben umgesetzt bzw. verifiziert.

## Suitenlauf (vollständig, nach Behebung)

- `npm run test`: **436/436 grün** (25 Testdateien).
- `npx playwright test` (alle Specs, Chromium): **165/165 grün**, davon 24 in
  `e2e/F-16-verbindungsvorgang.spec.ts` (vorher 164/165 — der eine rote Test ist der oben
  korrigierte).
- `npm run check`: **0 Fehler** (376 Dateien, 20 vorbestehende bzw. musterkonforme Warnungen,
  siehe Abschnitt 3).

## Ergebnis

Eine Abweichung gefunden (fehlender Hover vor der Labelprüfung, FR-45) und wie vom
Orchestrator vorgegeben behoben. Keine weiteren Befunde, keine offenen Quellenwidersprüche.

**Keine Abweichungen.**
